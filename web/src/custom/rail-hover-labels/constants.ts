/** Fork-only constants for the left-rail hover label overlay. */
export const TOOLBAR_EXPANDED_WIDTH = 180;
/** Delay before collapsing so the cursor can cross items. */
export const RAIL_LEAVE_DELAY_MS = 150;
/** Width/shadow/label ease-out for the expand overlay. */
export const RAIL_EXPAND_MOTION = "350ms cubic-bezier(0.22, 1, 0.36, 1)";
